## Some Bug Fixes
- When I new chat and give first prompt then until AI generate the title you title the chat as first prompt so if AI fails at least the chat is saved and when it generates the title just update it
- When I chat and immidelty start a new chat by the + button I cant fill input in the input box maybe becuse AI is generating reposne in that previous chat but I dont want that different chat sessions should not block each other so fix it
- And when I chat to model and while it generating response I start a new chat the chat will not be saved because AI is budy in generating title and the response of AI comes into the new chat, fix this also
- Also that Web search is just shit, its just a duc duk go search result, I want a web search like gemini, chatgpt, perplexity, for that use firecrawl for search and scraper and also in the manage API add a web search section and add providers and api key just like in model providers, because later we add more web search providers, anyways I add my firecrawl API in .env
- and one more thing if web search is enabled by user, and lets asuume it asks "tell me latest news" then instead of directly give this query for searching urls and scraping, give it to the model itself with the full context history and ask it to generate 2-3 queries for searching on web like it can respond "latest news on tech", "latest news headlines", "latest sports news" and then after getting the response search for these queries on web and scrape the urls by firecrawl api and give it to the model to respond
- You can refer to firecrawl docs at https://docs.firecrawl.dev/api-reference/v2-introduction