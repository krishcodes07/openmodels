## Their were some problems
1. When I chat and regenerate a response and then send another msg the response of it does not show the streaming was shown but when the full response is generated it hides but when I refresh and go in the chat again that msg shows, fix this and sometimes it happens with even regenrating
2. Sometimes AI gives 2-3 sources also in para or line but it cant show the icon of both, for single it shows only in the chat, fix this display every source icon
like Egypt has uncovered a lost Byzantine-era city in the western desert [Source 2, Source 5]. for 2 sources it does not give icon make it give icon and when clik on it redirect to the link
3. The vision models of all providers working, expect providers Nvidia, and cloudflare workers some models err this:
⚠️ Error generating response: 403 status code (no body)
⚠️ Error generating response: 404 status code (no body)
If it's API issu then no problem only 2-3 models aren't working
4. You are storing the uploaded image by the user in server/uploads, save it in the database of chat
5. The regenerate only gives AI repsonse but if I web search on then it not search on web to give response also fix this
6. Remove the deep think option from the thinking models beacuse whether it is on or not the AI thinks because it is a thinking model only, but you can show think to the models if it support on and off thinking
7. When AI streams the reponse, in UI it auto scroll to upwards which is good, but the problem is when I try to scroll to down to see the msg it automatically goes upward again where AI is streaming response fix this I can scroll down to read but when I scroll down to see you can show a down arrow option above the input box and when I click on it it will auto scroll to the bottom, and it will be not only while it streaming response but permanent
8. While AI generating the title for chat, it will name the chat as the user input but I can see it live saving chat in the UI, I can only see it when I refresh make it instant appears in the chat and when AI generated the title update it and reflect in the UI also.
9. Also add a copy icon below the user input box on the right side which will be only shown when user hovers on it and when clicking that button the user input or the prompt given by user will be copied
10. That's all for now make sure each thing would be fixed