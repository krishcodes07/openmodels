## What to do
- When the AI outputs HTML, CSS, JavaScript, React components, or SVGs, extract them and display them in a side-by-side interactive preview tab (running inside a sandboxed iframe). It also opens from the right side where there are two tabs "Code" and "Preview" and the preview is interactive, means you can interact with the code and it will reflect the changes in the preview.
- Why: It makes writing and testing code with the AI incredibly interactive and visual, just like Claude's Artifacts.

- Before writing code search on web that how these platforms do this