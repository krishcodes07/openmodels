## Edit Message Feature
Currently, we can regenerate the last message but we can't edit any messages.

## Implementation Plan
1. Add an edit icon to each message shown when hovers on it on laptops etc. but show it always on mobile view
2. When the user clicks the edit icon, open an edit modal with the message content.
3. When the user saves the edit, update the message content and regenerate the response starting from that message.  Just like chatgpt, gemini etc.
4. Show it on below of user message and also shift the copy icon there, so their will be 2 icons below the user message: copy and edit.

## One More Small thing
Like msg history was saved in chats but when I go to any of my previous chat and do chat in it make it it will apeear at the top of all chats, I means sort the chats according to the last message time. Like in other AI's.
Also add feature of pinning chats user can pin chats, and those will apeear at the top with Pinned:
