## Actually the vision feature is not working
- When I add image in the prompt and ask it, it response  Error generating response: 400 status code (no body) when I use gemini for vision and if nvidia it says ⚠️ Error generating response: 500 Internal Server Error and also ⚠️ Error generating response: 404 status code (no body)
- Search properly how vision input given in the provider
- Search their docs properly

## UI Fix
- Also when I send input with image, the image gets too big in chat make it smmaler and it sppears in a box left side and right side become empty make it start from right with only 2px padding around it and if their were more image add it to left and so on, like I have uplaoded the refrence

