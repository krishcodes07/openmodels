## Fixes
- In the Settings Page>Manage APIs what you have done is adding each provider name and below it an input box to add its API, this is not looking good and it is not good for user Experience instead only show Provider and API Key input, user can select a provier and add its API and save, he can later also see its API's
- The Vision thing is not working I am adding image but the AI is not responding anything, fix the issue
- Also I did not see the web search icon in the input area, web search should be for all models it is like a tool
- Instead of manually fformatting AI MD reponse you can use any free code or something which formats all the things in the chat (you search on web)
- And if model return any error or cant generate response then send it in the chat so ateast user know whats the problem
- thats for now