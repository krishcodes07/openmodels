## Chat
- I want that after AI response below it their will be icon of copy and regenrate button and when we click on it the AI generates another response in same chat for the same query and hide the first response generated and if user has regenrated the response then their will be <2/2>(example1/example2) icon for seeing the previous and next response.


## Web Search
The web search works well. I am suggesting one more thing which will make it more good:

- When AI response also add that sources button below the AI reponse after the Copy and regenerate button. And when user click on it, a sidebar will open on the right side where it will show the list of sources that AI used to generate the response + their logo + website name below it title of the news and description of it below it, like I have uploaded the reference image to take insipiration from.
their will be a X icon the top right of the opened sidebar for closing it, and it can be only opened by clicking on sources button

## Adding logic for new feature
We are implementing the logic for Usage and Analytics on settings page, where user can see their usage and analytics for ex- total chats, total tokens spend, total web searches also for each provider usage and and when click on it below it model wise token spend, etc.

we have to store the usage of user in database


## One more thing
You forgot to store the image uploaded by user in chat in the database, we have to store it, and user can only upload one image, make it can upload many image at a single time not one by one